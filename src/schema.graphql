#
# QUERIES
#
type Query {
    posts(sort: String!, limit: Int): [Post!]
}


#
# TYPES
#
type Post {
    content: String!
    id: String!
    name: String!
    subReddit: String!
    title: String!
    type: String!
}
